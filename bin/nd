#!/bin/bash

CURRENT_PATH=$(readlink -f "$0")
CURRENT_DIR=$(dirname "$CURRENT_PATH")

# Call the Python script with all command line arguments
python "$CURRENT_DIR"/../helper-scripts/parsedates.py "$@"
